# 🚀 Quick Start Guide - Polymarket Arbitrage Bot

## Пошаговая инструкция для запуска бота

### Шаг 1: Установка зависимостей ✅

```bash
pip install -r requirements.txt
```

**Проверка:**
```bash
python -m pytest -v
# Должно быть: 53 passed
```

---

### Шаг 2: Настройка .env 🔧

```bash
cp .env.example .env
```

Отредактируй `.env`:

#### 2.1 Обязательные параметры (без них бот не запустится):

```env
# Ethereum приватный ключ (для подписи ордеров на Polymarket)
PRIVATE_KEY=0xYOUR_PRIVATE_KEY_HERE

# API ключи бирж (если требуются)
POLYMARKET_API_KEY=your_key_here
SX_API_KEY=your_key_here
```

#### 2.2 Параметры риска (КРИТИЧНО):

```env
# Минимальная прибыль (в базисных пунктах, 1 bps = 0.01%)
# ВАЖНО: Используй 100+ bps для первых тестов!
MIN_PROFIT_BPS=100.0

# Максимальный размер позиции
# ВАЖНО: Начни с $5-10!
MAX_POSITION_SIZE=10.0

# Баланс на биржу (виртуальный лимит безопасности)
EXCHANGE_INITIAL_BALANCE=20.0
```

#### 2.3 Режим торговли:

```env
# false = симуляция (по умолчанию)
# true = реальная торговля (ОСТОРОЖНО!)
ENABLE_REAL_TRADING=false
```

#### 2.4 Алерты (рекомендуется):

```env
# Telegram (опционально)
TELEGRAM_BOT_TOKEN=your_telegram_bot_token
TELEGRAM_CHAT_ID=your_chat_id

# ИЛИ Discord (опционально)
DISCORD_WEBHOOK_URL=your_discord_webhook_url
```

---

### Шаг 3: Проверка конфигурации ✅

```bash
python scripts/preflight_check.py
```

**Ожидаемый результат:**
- ✅ .env file exists
- ✅ PRIVATE_KEY is set
- ✅ Wallet loaded successfully
- ✅ API keys configured
- ⚠️ Warnings (нормально для тестирования)

---

### Шаг 4: Найти реальные market IDs 🔍

#### 4.1 Поиск рынков:

```bash
python scripts/find_markets.py --limit 5
```

Выберешь market IDs с:
- Высокой ликвидностью (liquidity > $10,000)
- Активным объёмом торговли
- Актуальной датой окончания

#### 4.2 Тест коннекторов:

```bash
# Polymarket
python scripts/check_polymarket_connector.py <market_id>

# SX
python scripts/check_sx_connector.py <market_id>

# Kalshi (если используешь)
python scripts/check_kalshi_connector.py <ticker>
```

**Ожидаемый результат:**
- ✅ Successfully fetched orderbook
- ✅ Has valid bid and ask prices
- ✅ Bid < Ask (normal market)
- ✅ Has liquidity

#### 4.3 Обнови main.py:

Открой `main.py` и замени:

```python
pm_market_id = "REPLACE_WITH_REAL_POLYMARKET_MARKET_ID"
sx_market_id = "REPLACE_WITH_REAL_SX_MARKET_ID"
```

на реальные IDs, которые ты нашёл.

---

### Шаг 5: Тестовый запуск (симуляция) 🧪

```bash
# Запуск на 5 минут в режиме симуляции
python scripts/test_run.py --duration 300
```

**Что проверить:**
- ✅ Бот запустился без ошибок
- ✅ Получает данные о стаканах
- ✅ Находит арбитражные возможности (или сообщает что не нашёл)
- ✅ Симулирует сделки (status: 'simulated')
- ❌ НЕТ циклических ошибок

**Логи находятся в:**
- `logs/bot.log` - основной лог
- `logs/trades.log` - лог сделок
- `logs/statistics/opportunities.csv` - статистика

---

### Шаг 6: Анализ статистики 📊

```bash
# Просмотр статистики за последние 7 дней
python scripts/view_statistics.py --days 7

# Детальная статистика
python scripts/view_statistics.py --days 7 --detailed
```

**Что проверить:**
- Сколько возможностей найдено
- Средний спред (должен быть > комиссий)
- Expected PnL положительный
- Нет массовых ошибок

---

### Шаг 7: Микро-трейдинг ($5-10) 💰

**ТОЛЬКО ПОСЛЕ успешной симуляции!**

#### 7.1 Настрой .env:

```env
MAX_POSITION_SIZE=5.0          # $5 для первого теста
MIN_PROFIT_BPS=100.0           # Консервативный порог
ENABLE_REAL_TRADING=true       # ⚠️ РЕАЛЬНЫЕ ДЕНЬГИ!
```

#### 7.2 Запуск:

```bash
python scripts/test_run.py --real --position-size 5 --duration 1800
```

При запросе подтверждения введи: `YES`

#### 7.3 Мониторинг:

- Следи за логами в реальном времени
- Проверяй на биржах, что ордера реально проходят
- Сравнивай цены и объёмы

#### 7.4 Проверка на биржах:

**На каждой бирже вручную проверь:**
- Ордер прошёл по ожидаемой цене?
- Объём соответствует ожидаемому?
- Комиссии учтены правильно?

#### 7.5 Сверка PnL:

```bash
python scripts/view_statistics.py --days 1 --detailed
```

Сравни:
- Expected PnL vs Actual PnL
- Если разница > 10% - проверь расчёт комиссий

---

### Шаг 8: Постепенное масштабирование 📈

**ТОЛЬКО если микро-трейдинг успешен 2-3 дня!**

#### План постепенного увеличения:

1. **День 1-3:** $5 per trade
2. **День 4-7:** $10 per trade (если PnL положительный)
3. **День 8-14:** $25 per trade (если success rate > 80%)
4. **День 15+:** $50-100 per trade (если всё стабильно)

#### Обновление параметров:

```env
MAX_POSITION_SIZE=25.0         # Увеличивай постепенно
MIN_PROFIT_BPS=50.0            # Можешь снизить порог после тестов
```

---

## 🔥 Критические правила

### ⛔ НЕ ДЕЛАЙ:

1. **НЕ** включай `ENABLE_REAL_TRADING=true` без тестирования
2. **НЕ** начинай с больших позиций (> $10)
3. **НЕ** запускай без алертов в продакшене
4. **НЕ** игнорируй ошибки в логах
5. **НЕ** используй фейковые market IDs
6. **НЕ** торгуй рынки с низкой ликвидностью (< $1000)

### ✅ ДЕЛАЙ:

1. ✅ Начинай с симуляции
2. ✅ Тестируй каждый коннектор отдельно
3. ✅ Проверяй статистику ежедневно
4. ✅ Масштабируйся постепенно
5. ✅ Настрой алерты (Telegram/Discord)
6. ✅ Сверяй PnL с биржами вручную

---

## 📁 Структура файлов

```
.
├── .env                        # Твоя конфигурация (НЕ коммить!)
├── main.py                     # Основной entry point
├── config.py                   # Чтение настроек из .env
├── core/
│   ├── processor.py            # Логика поиска арбитражей
│   ├── trader.py               # Исполнение сделок
│   ├── statistics.py           # 📊 Сбор статистики (НОВОЕ)
│   └── ...
├── connectors/
│   ├── polymarket.py           # Коннектор Polymarket
│   ├── sx.py                   # Коннектор SX
│   └── kalshi.py               # Коннектор Kalshi
├── scripts/
│   ├── preflight_check.py      # ✅ Проверка конфига
│   ├── test_run.py             # 🧪 Безопасный тест
│   ├── find_markets.py         # 🔍 Поиск market IDs (НОВОЕ)
│   ├── check_*_connector.py    # 🔌 Тест коннекторов (НОВОЕ)
│   └── view_statistics.py      # 📊 Просмотр статистики (НОВОЕ)
├── logs/
│   ├── bot.log                 # Основной лог
│   ├── trades.log              # Лог сделок
│   └── statistics/             # 📊 CSV со статистикой (НОВОЕ)
└── tests/                      # Юнит-тесты (53 теста)
```

---

## 🆘 Частые проблемы

### Проблема: "Market IDs not configured"

**Решение:**
```bash
python scripts/find_markets.py
# Выбери market ID с высокой ликвидностью
# Обнови main.py
```

### Проблема: "No module named 'aiohttp'"

**Решение:**
```bash
pip install -r requirements.txt
```

### Проблема: "Invalid private key"

**Решение:**
- Убедись что ключ начинается с `0x`
- Проверь, что это действительно приватный ключ (64 hex символа)

### Проблема: "Insufficient balance"

**Решение:**
- Проверь реальный баланс на биржах
- Обнови `EXCHANGE_INITIAL_BALANCE` в .env

### Проблема: "No arbitrage opportunity found"

**Это нормально!** Арбитражные возможности редки.
- Проверь, что спреды на рынках перекрываются
- Убедись, что `MIN_PROFIT_BPS` не слишком высокий
- Попробуй другие рынки

---

## 📚 Дополнительные документы

- `PRODUCTION_VERIFICATION_REPORT.md` - Полный отчёт о готовности к продакшену
- `PRODUCTION_SETUP.md` - Детальная инструкция по развёртыванию
- `scripts/README.md` - Описание всех скриптов

---

## 🎯 Чек-лист готовности к продакшену

Перед запуском с реальными деньгами убедись:

- [ ] Все 53 теста проходят
- [ ] Preflight check проходит без критических ошибок
- [ ] Реальные market IDs настроены
- [ ] Все коннекторы протестированы в изоляции
- [ ] Симуляция работает 4+ часов без ошибок
- [ ] Алерты настроены (Telegram или Discord)
- [ ] Параметры риска консервативны (MIN_PROFIT_BPS ≥ 50)
- [ ] Размер позиции мал (MAX_POSITION_SIZE ≤ 10)
- [ ] Ты понимаешь логику работы бота
- [ ] Ты проверил фактический PnL вручную на биржах

**Если хоть один пункт не выполнен - НЕ ВКЛЮЧАЙ реальную торговлю!**

---

## 🤝 Поддержка

Если что-то не работает:

1. Проверь логи: `tail -f logs/bot.log`
2. Запусти preflight check: `python scripts/preflight_check.py`
3. Проверь статистику: `python scripts/view_statistics.py`
4. Протестируй коннектор отдельно

Удачи! 🚀
