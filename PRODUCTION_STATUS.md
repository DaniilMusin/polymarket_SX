# Production Status

**Last Updated:** 2025-12-02
**Current Status:** 🟡 **SIMULATION-ONLY** (не готово к реальной торговле)

---

## Текущее состояние

### Что работает ✅

- **Базовая логика арбитража** - протестирована, unit tests проходят
- **Риск-менеджмент** - ExchangeBalanceManager с thread-safe управлением балансами и экспортом в метрики
- **Runtime валидация** - `validate_all()` блокирует опасные конфигурации до запуска бота
- **Double-confirm** - реальный режим требует явного ввода `YES`
- **Коннекторы** - Polymarket, SX, Kalshi с retry логикой
- **Статистика** - сбор метрик в `core/statistics.py`, просмотр через `scripts/view_statistics.py`
- **Preflight checks** - `scripts/preflight_check.py` для валидации конфигурации

### Критические проблемы ❌

1. **Пока только симуляция** - нет подключения к реальным балансам и нет персистентного состояния
2. **Placeholder market IDs** - `main.py` содержит заглушки, запуск без замены завершится ошибкой
3. **Ограниченная наблюдаемость** - минимальные метрики, нет алертов или персистентных логов сделок
4. **Нет персистентности сделок** - после рестарта состояние арбитражей и позиций теряется

---

## Поддерживаемые точки входа

### ✅ `scripts/test_run.py` - РЕКОМЕНДУЕТСЯ

**Для чего:** Безопасный запуск с защитными параметрами

**Как использовать:**
```bash
# Simulation mode (безопасно)
python scripts/test_run.py --duration 300

# Real trading (требует подтверждения "YES")
python scripts/test_run.py --real
```

**Защиты:**
- Переопределяет `MAX_POSITION_SIZE=10`
- Переопределяет `MIN_PROFIT_BPS=100`
- Dry-run по умолчанию
- Double-confirm для `--real` режима
- Управление длительностью через `--duration`

### ⚠️ `main.py` - ИСПОЛЬЗОВАТЬ ОСТОРОЖНО

**Для чего:** Прямой запуск бота

**Особенности:**
- Выполняет runtime-валидацию через `validate_all()`
- Требует ручной замены market IDs (защита от случайного запуска)
- Требует double-confirm при `ENABLE_REAL_TRADING=true`

**Как использовать:**
```bash
# 1. Отредактировать market IDs в main.py
# 2. Проверить .env параметры
# 3. Запустить
python main.py
```

### ❌ Устаревшие / Не поддерживаются

- `main_improved.py` - экспериментальный, fake market IDs
- Любые другие "черновые" entrypoints

---

## Workflow для безопасного использования

### 1. Первоначальная настройка

```bash
# Скопировать .env.example
cp .env.example .env

# Отредактировать .env:
# - MIN_PROFIT_BPS=50 (минимум!)
# - MAX_POSITION_SIZE=10 (для начала)
# - ENABLE_REAL_TRADING=false
# - Добавить API ключи (если есть)
```

### 2. Preflight проверка

```bash
python scripts/preflight_check.py
# Убедись что все критические параметры OK
```

### 3. Поиск рынков (опционально)

```bash
# Найти ликвидные рынки на биржах
python scripts/find_markets.py

# Проверить конкретные коннекторы
python scripts/check_polymarket_connector.py
python scripts/check_sx_connector.py
```

### 4. Simulation запуск (ОБЯЗАТЕЛЬНО СНАЧАЛА)

```bash
# Запустить в dry-run на несколько часов
python scripts/test_run.py --duration 3600

# Посмотреть статистику
python scripts/view_statistics.py
```

### 5. Micro-trading (только если simulation успешна)

```bash
# С явным подтверждением
python scripts/test_run.py --real
# Когда спросит, ввести "YES" (capslock)
```

---

## Риск-параметры

### Для simulation (текущие дефолты):
```env
MIN_PROFIT_BPS=50.0          # 0.5% безопасный минимум
MAX_POSITION_SIZE=10.0       # Ограничение позиции по умолчанию
ENABLE_REAL_TRADING=false
```

### Для первого real trading:
```env
MIN_PROFIT_BPS=50.0          # 0.5% минимум (лучше 100)
MAX_POSITION_SIZE=10.0       # Начать с $10
ENABLE_REAL_TRADING=true     # Только после тестов!
```

### Для production (НЕ СЕЙЧАС):
```env
MIN_PROFIT_BPS=100.0         # 1% после статистики
MAX_POSITION_SIZE=50.0       # Увеличивать постепенно
```

---

## Что нужно ПЕРЕД real trading

- [ ] Собрать статистику в simulation на 2-3 дня
- [ ] Проанализировать `logs/statistics/opportunities.csv`
- [ ] Понять частоту находимых арбитражей
- [ ] Проверить соответствие ожидаемого и фактического PnL
- [ ] Настроить алерты (Telegram или Discord)
- [ ] Проверить API ключи на всех биржах
- [ ] Убедиться что wallet и private key валидны

---

## Known Limitations

### SX connector
- Парсинг API может быть неполным (placeholder код в `find_markets.py`)
- Рекомендуется найти market IDs вручную через сайт SX

### Статистика
- Собирается в CSV (`logs/statistics/opportunities.csv`, `trades.csv`)
- Нет автоматической агрегации за период
- Используй `scripts/view_statistics.py` или Excel для анализа

### Alerts
- File-based логирование работает всегда
- Telegram/Discord требуют настройки в `.env`

---

## История статусов

| Дата | Статус | Комментарий |
|------|--------|-------------|
| 2025-11-23 | 🟡 SIMULATION-ONLY | Базовая логика работает, нужна статистика и runtime валидация |
| 2025-11-22 | 🔴 NOT PRODUCTION READY | Найдены критические проблемы с ENABLE_REAL_TRADING |
| 2025-11-21 | 🟢 READY (ложно) | Старый отчёт был слишком оптимистичен |

---

## Архивированные документы

Старые отчёты перенесены в `docs/archive/`:
- `FINAL_PRODUCTION_REPORT_ARCHIVED.md` - устаревший, слишком оптимистичный
- `PRODUCTION_VERIFICATION_REPORT_ARCHIVED.md` - более реалистичный, но также устарел

**Используй только этот файл** (`PRODUCTION_STATUS.md`) как источник правды.

---

## Следующие шаги (приоритет)

1. ✅ Создать единый статус файл (этот документ)
2. 🔄 Добавить runtime валидацию рисков (`core/validation.py`)
3. 🔄 Добавить double-confirm в `main.py`
4. 🔄 Почистить устаревшие entrypoints
5. ⏳ Собрать статистику в simulation режиме (2-3 дня)
6. ⏳ Проанализировать результаты
7. ⏳ Только после анализа - рассмотреть micro-trading

---

**ВАЖНО:** Не пытайся "допилить код до идеала". Фокусируйся на статистике и понимании реального поведения бота.
